# Tech Review 1 多结构 XLSX 检视报告

本报告由 `tests/run_review_matrix.py` 在创建 XLSX、调用项目生成器并立即执行独立静态检视后生成。测试 XLSX 和对应 Verilog 均保存在各案例目录中。

## 汇总

| 案例 | 结果 | 分类 | 预期与实际 |
|---|---|---|---|
| `01_basic` 基础 TOP 与单子模块 | 通过 | 无问题 | 成功生成 2 个 Verilog；TOP output 由子模块驱动，未连接 input 置零、output 悬空 |
| `02_parameter_macro_shifted` 偏移表头、宏/parameter 与两个子模块 | 通过 | 无问题 | 成功生成 3 个 Verilog；非固定行列识别、宏、parameter 透传和内部 wire 均正确 |
| `03_duplicate_port_rows` 同一模块重复端口行 | 通过 | 无问题 | 成功生成 2 个 Verilog；重复行合并；TOP 与子模块同名 aaa 不会自动相连，子模块端生成 .aaa(2'b0) |
| `04_invalid_width` 非法位宽文本 | 通过 | 生成的 XLSX 问题 | 按预期拒绝生成，并定位到非法位宽单元格 |
| `05_unused_inout` 未连接的 inout 端口 | 通过 | 项目定义的歧义 | 成功生成 2 个 Verilog；当前实现将未连接 inout 生成为 .pad() |
| `06_width_mismatch` 子模块互连位宽不一致 | 通过 | 生成的 XLSX 问题 | 成功生成 3 个 Verilog；wire 采用 output 驱动端 8 bit，并产生指定格式 warning |

## 逐项检视

### 01_basic — 基础 TOP 与单子模块

- 生成文件：`basic_child.v`, `top_basic.v`
- 分类：无问题
- 结论：通过；成功生成 2 个 Verilog；TOP output 由子模块驱动，未连接 input 置零、output 悬空
- 诊断：``error[集成页签 集成 第 3 行: 内部连接只有 BASIC_CHILD.spare 一端，按未连接处理]``；``error[集成页签 集成 第 4 行: 内部连接只有 BASIC_CHILD.debug 一端，按未连接处理]``
- 静态检视：文件数量、模块边界、端口声明、实例端口唯一性和 output 赋零均符合预期。

### 02_parameter_macro_shifted — 偏移表头、宏/parameter 与两个子模块

- 生成文件：`consumer.v`, `producer.v`, `top_param.v`
- 分类：无问题
- 结论：通过；成功生成 3 个 Verilog；非固定行列识别、宏、parameter 透传和内部 wire 均正确
- 诊断：``warning[集成页签 Integration 第 5 行: PRODUCER.WIDTH parameter 应该被拉到 TOP，但当前没有显式 parameter 链接；TOP 中保留该 parameter 名称]``；``error[集成页签 Integration 第 5 行: 内部连接只有 PRODUCER.debug 一端，按未连接处理]``
- 静态检视：文件数量、模块边界、端口声明、实例端口唯一性和 output 赋零均符合预期。

### 03_duplicate_port_rows — 同一模块重复端口行

- 生成文件：`dup_child.v`, `top_dup.v`
- 分类：无问题
- 结论：通过；成功生成 2 个 Verilog；重复行合并；TOP 与子模块同名 aaa 不会自动相连，子模块端生成 .aaa(2'b0)
- 诊断：``error[集成页签 集成 第 3 行: 内部连接只有 DUP_CHILD.aaa 一端，按未连接处理]``
- 静态检视：文件数量、模块边界、端口声明、实例端口唯一性和 output 赋零均符合预期。

### 04_invalid_width — 非法位宽文本

- 生成文件：无（按预期拒绝生成）
- 分类：生成的 XLSX 问题
- 结论：通过；按预期拒绝生成，并定位到非法位宽单元格
- 诊断：``error[页签 BAD_WIDTH 第 3 行: 不支持的位宽 '7:0'；请使用非负整数、`MACRO 或 PARAMETER]``；``warning[未检测到集成页签，将只生成模块桩文件]``
- 静态检视：生成按预期中止，没有产生需要检视的 Verilog 文件。

### 05_unused_inout — 未连接的 inout 端口

- 生成文件：`io_child.v`, `top_io.v`
- 分类：项目定义的歧义
- 结论：通过；成功生成 2 个 Verilog；当前实现将未连接 inout 生成为 .pad()
- 诊断：``error[集成页签 集成 第 3 行: 内部连接只有 IO_CHILD.pad 一端，按未连接处理]``
- 静态检视：文件数量、模块边界、端口声明、实例端口唯一性和 output 赋零均符合预期。

### 06_width_mismatch — 子模块互连位宽不一致

- 生成文件：`top_width.v`, `width_dst.v`, `width_src.v`
- 分类：生成的 XLSX 问题
- 结论：通过；成功生成 3 个 Verilog；wire 采用 output 驱动端 8 bit，并产生指定格式 warning
- 诊断：``warning[WIDTH_SRC.payload信号和WIDTH_DST.payload信号应该连接，但是其位宽不匹配]``
- 静态检视：文件数量、模块边界、端口声明、实例端口唯一性和 output 赋零均符合预期。

## 分类结论

- 脚本问题：未发现。
- 生成的 XLSX 问题：案例 04 的位宽文本不符合已定义规则；案例 06 故意设置了互连位宽不匹配，脚本已按要求告警并继续生成。
- 项目定义的歧义：需求只规定未连接 input 赋零、output 悬空，未规定 inout。案例 05 当前采用悬空 `.pad()`，需由项目决定是否改为顶层引出或其他处理。
- 重复名称结论：案例 03 验证同一模块的重复端口行会合并为一个物理端口，不再产生重复定义警告，也不会生成重复的 Verilog 端口连接。
