追加需求：
当前我们的项目有几类新的输出
1.是带有{{i}}的信号，需要你进行展开，并且脚本有时候要推测其位宽，如果这种时候位宽不确定，你可以先比写成114（114表示：这个位宽的数非常有必要，但是为了让程序能正常运行，我先放一个114在这）
2.是多维数组和位宽中带有表达式，你应该自动计算表达式；同时，如果表达式中有 * （注意前后的空格）请你将其转换成多维数组
3.是interface
作为正式的Tech review，请你完成代码之后完成回归，并且进行3轮检视