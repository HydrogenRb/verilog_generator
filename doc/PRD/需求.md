读取test.xlse。
特定需求：读取各个页签，其中有模块定义和集成，创建带有简单GUI（甚至是terminal中的可以选择的就行）的**脚本**能自动生成对应数量的.v文件。
一个.v文件包含：io port，必要的wire，例化的模块。
1.位宽中带`的字符串为宏，不带的为parameter，输出先全部在模块内赋0；
2.读取集成页签，写TOP集成模块，PHY和TEST为子模块，B~G列标注了顶层模块和子模块的连接关系，J~M标注了子模块之间的连接关系，O~R标注了子模块不用链接的端口，响应的输入全部赋0，输出全部悬空。
请你完成一个脚本能自动执行上面的需求并且具有一定的扩展性，具有简洁文档。
你可以：
定义一些新的xlsx规则，询问我更多细节（最多两次）。
你应该：
完成脚本之后自己测试，脚本之后要进入不联网环境所以需要尽量少使用包，检视一轮生成之后的.v文件
你必须：
**完成**工作再退出