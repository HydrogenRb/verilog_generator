功能review
1.你的readme中有生成器会按当前模块中最长的字段统一排版的描述，但是项目中使用interface生成的名字没用对齐其他信号名

更多需求追加：
1.生成的时候考虑到分类，在生成的时候生成如下代码
```verilog
// ----- ----- ----- ----- ----- -----
// CLK & RST
// ----- ----- ----- ----- ----- -----
input  aaa;

// ----- ----- ----- ----- ----- -----
// CFG bus
// ----- ----- ----- ----- ----- -----
input  bbb;
output ccc;
```
如果没有分类并且在表格中全部紧挨着一起，则使用no group作为分类。
2.对于宏定义，直接使用`define，不需要使用ifndefine
3.对于宏定义、assign、wire等也保证对齐，不一定要全局对齐，但是至少宏定义和宏定义对齐，assign和assign对齐。
4.集成页面中模块的名字可以是直接的RISCV_CORE也有可能是module:RISCV_CORE
5.readme作为specification，请你针对性地写一些如何描述不同位宽，并且给几个例子，比如我的位宽是一个宏乘一个parameter并且*没有空格会如何生成？这种比较复杂、真实复杂项目的例子
6.定义的时候你有注释// Parameters local to child modules请你在保留英文的同时写一个中文的

追加项目：
请你交付一份**需求文档**给其他agent，目标是做一个新的软件，让链接这一步变成自动化和可视化，其吃进去三个module页签，和用户通过可视化进行交互（想象一下那种拉线的游戏）输出一个适用于当前项目的集成页签；

紧急中途追加需求：如果分类中有“条件：”当中的数据应该被放在一个ifdef的block中，其正常分类名可以是这个ifdef的数；这个宏没必要在开头定义